---
name: "DEFAULT"
import_path: 'joint_fail_sac_base.yml'
import_exp: 'DEFAULT'

params:
  mlp_actor: True

---
name: &name "no_joint_fail"
params:
  run_name: *name
  joint_fail_prob: 0.0
---
name: &name "test_local"
params:
  run_name: *name
  joint_fail_prob: 0.0
  timesteps: 1000
  save_interval: 1000
