---
name: "DEFAULT"
import_path: 'joint_fail_sac_base.yml'

params:
  mlp_actor: True

---
name: &name "no_joint_fail"
params:
  run_name: *name
  joint_fail_prob: 0.0

grid:
  provide_joint_fail_info_to_critic: [True, False]
